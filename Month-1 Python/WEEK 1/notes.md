➡️ Strings are Immutable
Why doesn't upper() change the original string?
Difference between strip() and replace()?
Difference between split() and join()?
Difference between find() and index()?
Why does split() return a list instead of a string?
